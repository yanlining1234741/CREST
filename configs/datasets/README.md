# Dataset configs

Generated by:

```bash
export CREST_DATA_ROOT=/path/to/mbeir_aligned
python run.py setup
```

Produces `flickr.yaml`, `mscoco.yaml`, `visualnews_task3.yaml` with absolute paths to your data root.

Do not commit machine-specific paths; regenerate after clone.
